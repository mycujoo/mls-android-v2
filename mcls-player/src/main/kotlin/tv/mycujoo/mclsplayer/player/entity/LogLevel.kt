package tv.mycujoo.mclsplayer.player.entity

/**
 * As part of SDK configuration, user may chose desired level of log outputs to console.
 */
enum class LogLevel {
    MINIMAL,
    INFO,
    VERBOSE
}
